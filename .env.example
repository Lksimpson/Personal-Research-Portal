# Copy to .env if you want to override defaults. Do not commit .env.
# Optional: override the Ollama model used for answer generation (default: gemma3:4b)
# OLLAMA_MODEL=gemma3:4b
# Optional: force keyword-only retrieval (default is semantic when index exists)
# SKIP_QUERY_EMBED=1
