# Copy to .env and set your keys. Do not commit .env.
# Local embeddings: 1 = sentence-transformers; fastembed = FastEmbed (ONNX, avoids segfault on Mac):
# USE_LOCAL_EMBEDDINGS=1
# USE_LOCAL_EMBEDDINGS=fastembed
# Use OpenAI for BOTH embed and generate (avoids Gemini 429 quota):
# USE_OPENAI=1
# Use OpenAI only for the answer step (embed can still use Gemini):
# USE_OPENAI_GENERATE=1
OPENAI_API_KEY=your_openai_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
